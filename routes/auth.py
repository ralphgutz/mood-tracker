from flask import Blueprint

auth_bp = Blueprint("auth", __name__)

# Auth routes are a bonus feature (B3) — stubbed for future implementation.
# When AUTH_ENABLED is False (default), these routes are not registered.
